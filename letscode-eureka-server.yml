server:
  port: 9091

eureka:
  instance:
    hostname: eureka-server-letscode.herokuapp.com
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 3000
